import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    alignItems: "center",
    borderRadius: 8,
    paddingHorizontal: 10,
    width: '100%',
    flex: 1,
  },

  input: {
    height: 40,
    borderColor: "gray",
    borderWidth: 1,
    borderRadius: 20,
    paddingHorizontal: 10,
    width: "100%",
    backgroundColor: "#fff",
  },
});
